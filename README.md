# Simple Tarantool Cartridge-based Key/Value storage

## Package app as Docker image

```bash
cartridge pack docker --tag tarantool-key-value-storage:latest
```

## Run docker compose

```bash
docker-compose up --build
```
